PACKAGES install AMD64
# irqbalance is normally pulled in as a recommendation by the kernel package.
# On KVM-based systems, this can cause performance problems, so we remove it.
linux-image-amd64 initramfs-tools irqbalance-

PACKAGES install DHCPC
isc-dhcp-client

PACKAGES install GRUB_PC
grub-pc
